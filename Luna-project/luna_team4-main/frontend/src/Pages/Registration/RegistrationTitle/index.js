import {RegistrationTitleP} from "./RegistrationTitle.sytle";

const RegistrationTitle = ({inputText}) => {

    return (
        <RegistrationTitleP>
            {inputText}
        </RegistrationTitleP>
    )
}

export default RegistrationTitle